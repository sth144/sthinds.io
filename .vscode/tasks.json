{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "upsert-user",
      "type": "shell",
      "command": "source ${workspaceFolder}/server/.env && ${workspaceFolder}/util/create_admin_user_dev.sh",
      "options": {
        "env": {
          "MONGODB_USERNAME": "${env:MONGODB_USERNAME}",
          "MONGODB_PASSWORD": "${env:MONGODB_PASSWORD}",
          "MONGODB_PORT": "${env:MONGODB_PORT}",
        },
        "cwd": "${workspaceFolder}/server",
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}