
language: node_js
node_js:
  - "v12.22.3"
sudo: required
before_install:
  - echo "Before install"
install:
  - echo "Install"
  - ls
  - pwd
  - npm install -g jest ts-jest typescript@latest react-scripts babel-jest@26.6.0
  - cd server
  - npm install
  - cd ../client
  - npm install -D jest ts-jest typescript@latest
  - npm install
  - cd ..
  - ls 
  - ls client/node_modules
  - ls server/node_modules
script:
  - ./test/test.sh
  # TODO: Docker build and upload